{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "runtimeVersion": "4.8.7", // If i need to run node 4.8.7
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/elrond-scanner.js"
    }
  ]
}
